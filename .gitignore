node_modules/
# Build output, except the bundled CLI a source install of the plugin runs
# (see scripts/check-cli-bundle.mjs). `dist/*`, not `dist/`, so the exception can apply.
**/dist/*
!packages/cyberlegion/dist/cli.mjs
.env
.env.local
*.tsbuildinfo
TODO.local.md
apps/web/.astro
.turbo/
.cyberfleet/
