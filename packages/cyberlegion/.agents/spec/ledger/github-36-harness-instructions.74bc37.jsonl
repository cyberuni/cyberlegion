{"seq":1,"kind":"leash","cr":"github-36-harness-instructions","leash":"auto-all","by":"derived","blast":"low","note":"headless unit dispatched by the standing owner; focused bugfix in one pure string builder; spec edit additive to a frozen suite (self-clears); the one policy call (cursor has no instruction channel, so refuse) was mailed to the owner as needs-input with a recommendation before build. Self-assert by:agent at both gates; the human ratification is the owner's at the PR.","approach":["per-harness-instruction-channel-verified-from-cli","claude-append-system-prompt","codex-developer-instructions-config-override","cursor-instructions-refuse-loudly","additive-scenarios-on-the-agent-node"]}
{"seq":2,"kind":"gate","cr":"github-36-harness-instructions","gate":"spec","verdict":"approve","by":"agent","cause":"dimension","frozen":["agent/agent.feature"],"why":{"floor":"none — additive only (gherkin-cli diff addOnly: 7 scenarios added, 0 modified or removed); the file stays @frozen and self-clears; no semver break (codex instructions that were rejected now arrive; the one new refusal is a cursor def with a body, which previously launched a command cursor rejects — patch-class); the cursor refusals specialize, not contradict, the general cursor rows (judge concurred).","blast":"low — one node (agent/), one prose paragraph, one map-table row, seven additive scenarios; root spec.md frontmatter untouched.","novelty":"low — mirrors the per-harness effort-channel realization already on this node.","confidence":"high — cold sdd-spec-judge round 1: oracle, builder and architect PASS, ALIGNED true, SCENARIOS_FAILING empty, no open markers, preflight pass. Its one CONTENT_GAP (the empty-body claim was tested only on cursor, asymmetric with the effort section) was folded in as a claude/codex Outline before freezing.","judge":"cold sdd-spec-judge round 1 — ALIGNED true; conformance warn on the pre-existing missing What/Control Flow/Scenario map sections (already filed as #38), non-blocking.","pending":"the cursor refusal policy was mailed to the owner as needs-input; if the owner picks another path, that scenario pair re-opens under Clearance."}}
{"seq":3,"kind":"gate","cr":"github-36-harness-instructions","gate":"impl","verdict":"approve","by":"agent","cause":"dimension","why":{"floor":"none — no frozen scenario touched by the impl; the diff stays inside realize.ts, its test, one devDependency and a changeset.","blast":"low — one private helper pair in one pure string builder; cli.ts and session.ts untouched.","novelty":"low — a per-harness spelling of a field already parsed, mirroring the effort helper.","confidence":"high — cold sdd-impl-judge round 1: IMPLEMENTATION_PASS true, 7/7 new frozen scenarios re-derived by hand, each backstopped by a scoped mutation that broke exactly its targets; no absorption; pre-existing cursor fixtures' empty-body change judged a required accommodation, not a weakening; full package 627 tests green.","judge":"cold sdd-impl-judge round 1 — IMPLEMENTATION_PASS true, scope clean.","stop":"headless unit — by:agent is provisional; the human ratification and the merge are the owner's at the PR; the cursor policy awaits the owner's needs-input reply."}}
{"seq":4,"kind":"followup","cr":"github-36-harness-instructions","class":"backlog","what":"cursor-agent has no instruction channel, so a cursor def with a body refuses; if a delivery path is wanted (workspace rule file, or instructions carried in the brief), decide it as its own change","evidence":"cursor-agent 2026.07.01 --help lists no system-prompt, instructions or rules flag and no config override"}
{"seq":5,"kind":"gate","cr":"github-36-harness-instructions","gate":"spec","verdict":"approve","by":"agent","cause":"dimension","frozen":["agent/agent.feature","unit/lifecycle/lifecycle.feature"],"why":{"floor":"none — additive only against main (agent +8, lifecycle +2; 0 modified or removed); both files stay @frozen and self-clear. The cursor path changed from refusal to prepend-to-brief by the owner's ratified decision; the earlier refusal scenarios were never merged, so no frozen scenario was narrowed. Semver patch-class.","blast":"low — two nodes (agent/, unit/lifecycle/), one CFG terminal step split into four branches, one new spawn decision, ten map rows.","novelty":"low — mirrors the per-harness effort realization; the brief prepend is one pure composition step.","confidence":"high — cold sdd-spec-judge round 3: oracle, builder, architect PASS, ALIGNED true, conformance pass, no open markers. Round 2 failed the architect lens on a stale return-shape description (recorded as a combat-log correction) and was remediated with a sweep. Round 3's one CONTENT_GAP (label vs prepended brief) folded in as prose before freezing.","judge":"cold sdd-spec-judge round 3 — ALIGNED true.","supersedes":"seq 2's cursor-refusal scenarios and seq 4's cursor follow-up: the owner decided the delivery path (prepend to brief), so the follow-up is resolved in this CR."}}
{"seq":6,"kind":"gate","cr":"github-36-harness-instructions","gate":"impl","verdict":"approve","by":"agent","cause":"dimension","why":{"floor":"none — no frozen scenario edited by the impl; diff in realize.ts, cli-input.ts, session.ts and their tests.","blast":"low — one pure helper per file (instructionArgs, composeBrief) and one pass-through field.","novelty":"low — mirrors the effort helper; the brief prepend is a pure composition.","confidence":"high — cold sdd-impl-judge round 2: IMPLEMENTATION_PASS true, 10/10 new frozen scenarios re-derived by hand, load-bearing branches backstopped by scoped mutations that each broke their target; no absorption. Its note that the new verifications did not bind through the scenario bridge (describe titles carried a suffix) was acted on: retitled to the bare node keys, and verify-scenarios now reports all 10 bound and PASS.","judge":"cold sdd-impl-judge round 2 — IMPLEMENTATION_PASS true, scope clean.","stop":"headless unit — by:agent is provisional; merge judgment rests with the operator."}}
